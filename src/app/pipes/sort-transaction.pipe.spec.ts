import { SortTransactionPipe } from './sort-transaction.pipe';

describe('SortTransactionPipe', () => {
  it('create an instance', () => {
    const pipe = new SortTransactionPipe();
    expect(pipe).toBeTruthy();
  });
});
